data_dir: "${oc.env:DATA_DIR,data}"

# DataLoader parameters
train_batch_size: 16
val_batch_size: 64
test_batch_size: 64
predict_batch_size: 64
num_workers: 1
pin_memory: true
persistent_workers: true
