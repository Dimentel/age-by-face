defaults:
  - base

dropout_rate: 0.3
use_batch_norm: true
activation: "relu"
output_activation: "relu"
init_method: "kaiming"
# model configuration
type: "resnet18"
pretrained: true
hidden_layers: [512] # list of hidden layer sizes
