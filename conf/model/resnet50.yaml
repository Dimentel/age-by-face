defaults:
  - base

dropout_rate: 0.3
use_batch_norm: true
activation: "relu"
output_activation: "relu"
init_method: "kaiming"
# model configuration
type: "resnet50"
pretrained: true
hidden_layers: [2048] # list of hidden layer sizes
